#include <stdio.h>

int main() {
  
  int i = 0;
  int m;

  scanf("%d", &m);

  int vetor[m];

  for(i = 0; i < m; i++){
    scanf("%d", &vetor[i]);
  }

    // for(j = 0; j < n-1; j++){ 
    //      printf("%d ", matriz[i][j]);
    //    }printf("%d", matriz[i][j]);
    //  } 

  
  for(i = m-1; i > 0; i--){
    printf("%d ", vetor[i]);
  }printf("%d", vetor[i]);
  
  return 0;
}
